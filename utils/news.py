def get_news(topic="world"):
    # stub headlines (replace with NewsAPI if you get a key)
    return [
        f"Top story on {topic}: markets react to new data.",
        f"{topic.title()} update: analysts optimistic.",
        f"Breaking {topic}: global trends continue."
    ]
